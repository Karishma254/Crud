const mongoose = require("mongoose")

const productschema = mongoose.Schema ( {
    time: { type:String}. microtime.now(),
    Date: {type:String}.microtime.now()
  }) 

  module.exports = mongoose.model("productschema", productschema)